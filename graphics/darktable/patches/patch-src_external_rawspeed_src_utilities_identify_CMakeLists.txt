$NetBSD$

--- src/external/rawspeed/src/utilities/identify/CMakeLists.txt.orig	2023-06-12 21:39:40.000000000 +0000
+++ src/external/rawspeed/src/utilities/identify/CMakeLists.txt
@@ -12,6 +12,8 @@ target_link_libraries(${rsidentify} raws
 
 target_link_libraries(${rsidentify} rawspeed_get_number_of_processor_cores)
 
+target_link_libraries(${rsidentify} "gcc_s")
+
 if(BUILD_TESTING)
   rawspeed_add_test(NAME utilities/${rsidentify} COMMAND ${rsidentify}
                     WORKING_DIRECTORY "$<TARGET_PROPERTY:rawspeed_get_number_of_processor_cores,BINARY_DIR>")
